document.addEventListener("DOMContentLoaded", () => {
    const ctaButton = document.getElementById("cta-button");

    ctaButton.addEventListener("click", () => {
        alert("Explore todas as páginas para saber mais sobre mim!");
    });
});
